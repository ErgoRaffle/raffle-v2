
export const raffleService = `{sigmaProp(true)}`;
  
export const inactiveRaffle = `{sigmaProp(true)}`;
  
export const raffleTicketRepo = `{sigmaProp(true)}`;

export const activeRaffle = `{sigmaProp(true)}`;
  
export const winnerTokens = `{sigmaProp(true)}`;
  
export const successRaffle = `{sigmaProp(true)}`;
  
export const ticket = `{sigmaProp(true)}`;
  
export const raffleWinner = `{sigmaProp(true)}`;
  
export const raffleRedeem = `{sigmaProp(true)}`;
  
export const tokenRedeem = `{sigmaProp(true)}`;
