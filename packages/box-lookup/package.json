{
  "name": "@ergo-raffle/box-lookup",
  "version": "0.0.0",
  "description": "Raffle v2 box lookup",
  "repository": {
    "type": "git",
    "url": "https://git.ergopool.io/ergo/ergoraffle/raffle-v2.git"
  },
  "license": "MIT",
  "author": "Ergo Raffle Team",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "clean": "rimraf dist && rimraf tsconfig.build.tsbuildinfo",
    "coverage": "npm run test -- --coverage",
    "lint": "eslint --fix . && npm run prettify",
    "lint:check": "eslint . && npm run prettify:check",
    "prettify": "prettier --write . --ignore-path ../../.gitignore",
    "prettify:check": "prettier --check . --ignore-path ../../.gitignore",
    "release": "npm run test -- --run && npm run clean && npm run build && npm publish --access public",
    "test": "cross-env NODE_OPTIONS=\"--import tsx\" vitest",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@rosen-bridge/abstract-logger": "^2.0.1",
    "@rosen-bridge/json-bigint": "^0.2.0",
    "@rosen-bridge/tx-pot": "^1.0.3",
    "@rosen-clients/ergo-node": "^1.2.0"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
