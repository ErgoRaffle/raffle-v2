/**
 * Ergo network type used by `box-lookup` for address (re)construction.
 * This is intentionally a small enum (not SDK-specific).
 */
export enum ErgoNetwork {
  Mainnet = 'mainnet',
  Testnet = 'testnet',
}
